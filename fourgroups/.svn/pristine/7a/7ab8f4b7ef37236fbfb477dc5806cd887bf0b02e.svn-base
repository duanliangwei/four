<?php
namespace app\index\model;
use think\Model;
use think\DB;
class Goods extends Model{
	//多文件上传
	public function details_add($path)
	{
		$arr = $path;
		echo $arr;die;
	   // $path = explode(',',$data);
       $res = Db::table('lr_details')->insert();
       // var_dump($res);
       // return $res;
	}
}